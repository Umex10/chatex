import MessagesPage from "./(chats)/messages/page";


/**
 * Default component renders the default chat messages page.
 * Used as a fallback or initial view in the chat route.
 */
export default function Default() {
  return <MessagesPage />;
}
